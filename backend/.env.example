# Database connection string for PostgreSQL
# On Render, this is automatically provided. For local development:
# DATABASE_URL=postgresql://user:password@localhost:5432/palliative
DATABASE_URL=

# Node environment
NODE_ENV=development

# Server port (Render sets this automatically, but you can override)
PORT=3000
